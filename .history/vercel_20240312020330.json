{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": { "distDir": "build" },
      "buildCommands": "next build",
      "framework": "nextjs"

    }
  ],
  "routes": [
    { "app": "/(.*)", "dest": "/" }
  ]
}
